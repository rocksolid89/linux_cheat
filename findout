в этом файле я буду складывать недоконца ясные мне программы

quotaoff -gv /pub


-- какая команда может изменить пары CR LF в импортированном файле на стандартные для линукса LF
	tr -d '\r' < file.txt > newfile.txt
