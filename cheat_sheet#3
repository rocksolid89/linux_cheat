#try to continue studying some basic linux features

this exam i passed with 30% score - it's bullshit !


# куда может быть установлен загрузчик ?
	MBR жесткого диска
	Boot сектор дискеты
	Boot сектор раздела жесткого диска

#Чтобы установить GRUB в MBR первого жесткого диска в системе
	grub install '(hd0)'

#Какая команда может использоваться для изменения фактора уступчивости (nice) выполняемых процессов
	renice (позволяет менять фактор уступчивости у процесса [диапазон как правило от -20 до 19. отрицательное значение - максимальное значание фактора уступчивости. процесс обладает наивысшим приоритетом]. Чит - если система сильно нагружена - нужно запустить процесс командной оболочки с низким коэффициентом уступчивости.
	top
	   запустить top -- 'r' -- ввести pid процесса -- ввести новое значение фактора уступчивости ( отрицательное - увеличение фактора уступчивости; положительное - значение фактора уступчивости будет меньше, чем у других процессов )

#Как вычислить текущий объем используемого раздела подкачки?
	top -b -n1 | grep -i swap
	cat /proc/meminfo | grep -i swap


#Какой восьмеричный код соответствует строке пермишнов rwxr-sr-x
	верный ответ 2755
		потому, что 4 - suid, 2 - sgid, 1 - sticky-bit

#Вы обрабатываете файл с большим количеством пустых строк. Какой командой можно вычистить пустые строки
	grep -v '^$' < file.txt
	  v - ключ означает 'invert maching' - то есть будет выведен весь текст, не соответствующий 'pattern to mach' 
	
#С помощью каких команд можно найти файлы в системе
	whereis, which, find, locate

#У вас очень много файлов в каталоге /tmp с разрешением *.foo. Каким образом их можно удалить ?
	ls | grep 'foo$'| xargs rm

#С помощью какой команды можно создать раздел, отформатировав его в файловую систему ext2 ?
	mke2fs   &   mkfs.ext2

